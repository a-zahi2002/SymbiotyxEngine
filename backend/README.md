# Backend

Core processing modules for SymbiotixEngine.

- `capture/` — Input capture and preprocessing
- `grammar/` — Grammar and syntax processing
- `intensity/` — Intensity analysis modules
- `models/` — ML/AI models
- `config/` — Configuration files
- `server/` — API server and endpoints
